import HomePage from "../page";

// Review-only: the home page with the poster hero variant. Delete once a
// hero is chosen.
export default function HeroPosterPreview() {
  return <HomePage heroVariant="poster" />;
}
